<!-- pages/home/index.wxml -->
<view class="page-container">


  <!-- 日期导航 (可选，保持在设计中以备用) -->
  <view class="date-navigator">
      <view class="nav-arrow" bindtap="goToPrevDay">‹</view>
      <view class="date-display" bindtap="showCalendar">
        <text class="date-text">{{formattedDate}}</text>
      </view>
      <view class="nav-arrow" bindtap="goToNextDay">›</view>
  </view>

  <!-- 2. 核心区：能量红绿灯 Hero Card -->
  <view class="hero-section">
    <view class="hero-card" data-status="{{heroStatus}}">
      <!-- 动态光晕 -->
      <view class="hero-glow"></view>
      
      <view class="hero-content">
        <text class="hero-label">今日能量平衡</text>
        <view class="hero-value-group">
            <text class="hero-value">{{todayData.calorieBalance > 0 ? '+' : ''}}{{todayData.calorieBalance}}</text>
            <text class="hero-unit">kcal</text>
        </view>
        
        <!-- 状态胶囊 -->
        <view class="hero-status-pill">
            <view class="status-dot"></view>
            <text class="status-text">{{heroStatusText}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 3. 数据双球 Stats Grid -->
  <view class="stats-grid">
    <!-- 饮食球 -->
    <view class="stat-card diet-card" bindtap="onViewNutritionDetail">
        <view class="mini-sphere diet-sphere">
            <view class="sphere-glass"></view>
            <!-- 液体高度根据百分比变化 -->
            <view class="liquid" style="height: {{progressMetrics.dietProgress}}%">
                <view class="wave-layer wave-1"></view>
                <view class="wave-layer wave-2"></view>
                <view class="wave-layer wave-3"></view>
            </view>
        </view>
        <view class="stat-info">
            <text class="stat-label">饮食摄入</text>
            <view>
                <text class="stat-num">{{todayData.dietCalories}}</text>
                <text class="stat-target">/ {{todayData.targetCalories}}</text>
            </view>
        </view>
    </view>

    <!-- 运动球 -->
    <view class="stat-card exercise-card" bindtap="onViewExerciseDetail">
        <view class="mini-sphere exercise-sphere">
            <view class="sphere-glass"></view>
            <view class="liquid" style="height: {{progressMetrics.exerciseProgress}}%">
                <view class="wave-layer wave-1"></view>
                <view class="wave-layer wave-2"></view>
                <view class="wave-layer wave-3"></view>
            </view>
        </view>
        <view class="stat-info">
            <text class="stat-label">运动消耗</text>
            <view>
                <text class="stat-num">{{todayData.exerciseCalories}}</text>
                <text class="stat-target">/ {{todayData.exerciseTargetCalories}}</text>
            </view>
        </view>
    </view>
  </view>

  <!-- 4. 行动胶囊 Actions -->
  <view class="actions-grid">
    <!-- 主操作：饮食记录 -->
    <view class="action-card primary" bindtap="onQuickAction" data-url="/pages/diet/index/index">
        <view class="action-glow"></view>
        <view class="action-content">
            <text class="action-label">饮食记录</text>
        </view>
    </view>

    <!-- 记运动 -->
    <view class="action-card primary" bindtap="onQuickAction" data-url="/pages/exercise/search/index">
        <view class="action-glow"></view>
        <view class="action-content">
            <text class="action-label">运动记录</text>
        </view>
    </view>

    <!-- 看计划 -->
    <view class="action-card" bindtap="onQuickAction" data-url="/pages/plan/detail/index">
        <view class="action-content">
            <text class="action-label">我的计划</text>
        </view>
    </view>

    <!-- 问 AI -->
    <view class="action-card" bindtap="onAISuggestion">
        <view class="action-content">
            <text class="action-label">AI分析</text>
        </view>
    </view>
  </view>

  <!-- 5. 体重趋势卡片 -->
  <view class="weight-card" bindtap="openWeightModal">
    <view class="weight-header">
      <view class="weight-title-group">
        <text class="weight-label">当前体重</text>
        <view class="weight-value-group">
          <text class="weight-value">{{weightData.current || '--'}}</text>
          <text class="weight-unit">kg</text>
        </view>
      </view>
      <view class="weight-change {{weightData.trend}}">
        <text class="change-text">{{weightData.changeText}}</text>
      </view>
    </view>
    
    <!-- 迷你折线图 -->
    <view class="mini-chart">
      <canvas canvas-id="weightChart" id="weightChart" class="chart-canvas" disable-scroll="true"></canvas>
    </view>
    
    <!-- 添加按钮 -->
    <view class="add-weight-hint">
      <text class="hint-icon">+</text>
      <text class="hint-text">点击记录今日体重</text>
    </view>
  </view>

  <!-- 6. AI 建议条 (悬浮) -->
  <view class="ai-floating-bar animate-fade-in" wx:if="{{aiInsight.message}}">
      <text class="ai-bar-icon">💡</text>
      <text class="ai-bar-text">{{aiInsight.message}}</text>
      <view class="ai-bar-close" catchtap="closeAISuggestion">
        <text class="close-icon">✕</text>
      </view>
  </view>

  <!-- ============ 日历选择器弹窗 ============ -->
  <view class="calendar-modal {{showCalendarModal ? 'show' : ''}}" catchtap="hideCalendar">
    <view class="calendar-content" catchtap="stopPropagation">
      <view class="calendar-header">
        <view class="calendar-nav" bindtap="prevMonth">
          <text>‹</text>
        </view>
        <text class="calendar-title">{{calendarYear}}年{{calendarMonth}}月</text>
        <view class="calendar-nav" bindtap="nextMonth">
          <text>›</text>
        </view>
      </view>

      <!-- 星期头 -->
      <view class="calendar-weekdays">
        <text class="weekday">日</text>
        <text class="weekday">一</text>
        <text class="weekday">二</text>
        <text class="weekday">三</text>
        <text class="weekday">四</text>
        <text class="weekday">五</text>
        <text class="weekday">六</text>
      </view>

      <!-- 日期网格 -->
      <view class="calendar-days">
        <block wx:for="{{calendarDays}}" wx:key="date">
          <view
            class="calendar-day {{item.isCurrentMonth ? '' : 'other-month'}} {{item.isToday ? 'today' : ''}} {{item.isSelected ? 'selected' : ''}} {{item.hasRecord ? 'has-record' : ''}} {{item.isFuture ? 'future' : ''}}"
            bindtap="{{item.isFuture ? '' : 'selectCalendarDay'}}"
            data-date="{{item.date}}"
          >
            <text class="day-number">{{item.day}}</text>
            <view class="record-dot" wx:if="{{item.hasRecord}}"></view>
          </view>
        </block>
      </view>

      <!-- 快捷选项 -->
      <view class="calendar-shortcuts">
        <view class="shortcut-btn" bindtap="selectToday">今天</view>
        <view class="shortcut-btn" bindtap="selectYesterday">昨天</view>
        <view class="shortcut-btn" bindtap="selectThisWeek">本周</view>
      </view>
    </view>
  </view>

  <!-- ============ 体重记录弹窗 ============ -->
  <view class="weight-modal {{showWeightModal ? 'show' : ''}}" catchtap="closeWeightModal">
    <view class="weight-modal-content" catchtap="stopPropagation">
      <!-- 弹窗头部 -->
      <view class="modal-header">
        <text class="modal-title">记录体重</text>
        <view class="modal-close" bindtap="closeWeightModal">
          <text>×</text>
        </view>
      </view>

      <!-- 体重显示 -->
      <view class="weight-display-section">
        <view class="weight-display-value">
          <text class="display-number">{{inputWeight || '0.0'}}</text>
          <text class="display-unit">kg</text>
        </view>
        <view class="weight-compare" wx:if="{{weightData.current}}">
          <text class="compare-label">上次记录：</text>
          <text class="compare-value">{{weightData.current}} kg</text>
        </view>
      </view>

      <!-- 滑动条输入 -->
      <view class="slider-input-section">
        <view class="slider-wrapper">
          <view class="slider-track">
            <view class="slider-fill" style="width: {{(inputWeight - 30) / 120 * 100}}%"></view>
          </view>
          <slider
            class="weight-slider"
            min="30"
            max="150"
            step="0.1"
            value="{{inputWeight}}"
            activeColor="transparent"
            backgroundColor="transparent"
            block-size="0"
            bindchange="onWeightSliderChange"
            bindchanging="onWeightSliderChanging"
          />
          <view class="slider-thumb" style="left: {{(inputWeight - 30) / 120 * 100}}%">
            <view class="thumb-dot"></view>
          </view>
        </view>
        
        <!-- 刻度 -->
        <view class="slider-marks">
          <text class="mark">30kg</text>
          <text class="mark">90kg</text>
          <text class="mark">150kg</text>
        </view>
      </view>

      <!-- 快捷输入 -->
      <view class="quick-weights">
        <view class="quick-label">快捷输入</view>
        <view class="quick-btns">
          <view class="quick-btn" bindtap="adjustWeight" data-delta="-0.5">-0.5</view>
          <view class="quick-btn" bindtap="adjustWeight" data-delta="-0.1">-0.1</view>
          <input class="direct-input" type="digit" value="{{inputWeight}}" bindinput="onWeightDirectInput" />
          <view class="quick-btn" bindtap="adjustWeight" data-delta="0.1">+0.1</view>
          <view class="quick-btn" bindtap="adjustWeight" data-delta="0.5">+0.5</view>
        </view>
      </view>

      <!-- 备注输入 -->
      <view class="note-section">
        <view class="note-label">备注（选填）</view>
        <input class="note-input" placeholder="今天感觉不错..." value="{{weightNote}}" bindinput="onNoteInput" />
      </view>

      <!-- 操作按钮 -->
      <view class="modal-actions">
        <view class="modal-btn cancel" bindtap="closeWeightModal">取消</view>
        <view class="modal-btn save" bindtap="saveWeight">保存</view>
      </view>
    </view>
  </view>

</view>
